import React from "react";

const passwordInput = (props) => {
  return (
    <div className="PasswordInput">
      <p>Password: {props.passwordInput}</p>
    </div>
  );
};

export default passwordInput;
